"use strict"

const configDB = {
    access: "access",
    category: "categories",
    dish: 'dishs',
    role: 'roles',
    user: "users",
    order: "orders",
    transaction: "transactions"
}

export default configDB;